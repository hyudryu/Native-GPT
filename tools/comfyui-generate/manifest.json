{
  "id": "comfyui-generate",
  "name": "ComfyUI Generate",
  "description": "Generates an image or video via ComfyUI running on a remote backend host. Submits a prompt to the ComfyUI workload, waits for the result, and returns the generated asset. REQUIRES user approval before each call.",
  "version": "1.0.0",
  "trusted": true,
  "default_enabled": true,
  "risk": "external_side_effect",
  "requires_approval": true,
  "network": "outbound",
  "timeout_seconds": 180
}
