{
  "id": "openvoice-register-voice",
  "name": "OpenVoice Register Voice",
  "description": "Registers a cloned voice from a reference audio clip (MP3/WAV). Uploads the clip to the OpenVoice workload, which extracts the speaker embedding. Returns a voice_id usable with openvoice_tts. REQUIRES user approval before each call.",
  "version": "1.0.0",
  "trusted": true,
  "default_enabled": true,
  "risk": "external_side_effect",
  "requires_approval": true,
  "network": "outbound",
  "timeout_seconds": 60
}
