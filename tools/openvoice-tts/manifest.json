{
  "id": "openvoice-tts",
  "name": "OpenVoice TTS",
  "description": "Synthesizes speech from text using OpenVoice on a remote backend host. Supports voice cloning via a registered voice_id. REQUIRES user approval before each call.",
  "version": "1.0.0",
  "trusted": true,
  "default_enabled": true,
  "risk": "external_side_effect",
  "requires_approval": true,
  "network": "outbound",
  "timeout_seconds": 60
}
